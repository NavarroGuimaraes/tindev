import React from 'react';
//Biblioteca do react que lida com a DOM (arvore de elementos) pra navegação
//no browser
import ReactDOM from 'react-dom';
import App from './App';

//Renderiza o app na root
ReactDOM.render(<App />, document.getElementById('root'));

